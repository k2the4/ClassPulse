import Link from 'next/link'; import { BarChart3, BookOpen, Plus, Settings, ShieldCheck } from 'lucide-react'; import type { Profile } from '@/lib/types';
export function Shell({profile,children}:{profile:Profile;children:React.ReactNode}) { const links=[['/dashboard','Dashboard',BarChart3],['/classes','Classes',BookOpen],['/classes/new','Add class',Plus],...(profile.role==='admin'?[['/admin','Admin',ShieldCheck] as const]:[])]; return <div className="min-h-screen md:flex"><aside className="border-b bg-white p-4 md:min-h-screen md:w-60 md:border-b-0 md:border-r"><Link href="/dashboard" className="mb-8 block text-xl font-bold text-blue-800">ClassPulse</Link><nav className="flex gap-2 md:block">{links.map(([href,label,Icon])=><Link key={href} href={href} className="mb-1 flex items-center gap-2 rounded-lg px-3 py-2 text-sm hover:bg-sky-50"><Icon size={17}/>{label}</Link>)}<Link href="/pending" className="flex items-center gap-2 rounded-lg px-3 py-2 text-sm hover:bg-sky-50"><Settings size={17}/>Account</Link></nav></aside><div className="min-w-0 flex-1"><header className="flex items-center justify-between border-b bg-white px-6 py-4"><div><p className="font-medium">{profile.full_name}</p><p className="text-xs capitalize text-slate-500">{profile.role.replace('_',' ')}</p></div><form action="/auth/signout" method="post"><button className="btn-secondary">Sign out</button></form></header><main className="p-4 md:p-7">{children}</main></div></div> }
